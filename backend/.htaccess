# Ficheiro: C:\Users\carlo\babyhappy\public\.htaccess

<IfModule mod_rewrite.c>
    RewriteEngine On

    # Use o nome da sua pasta do projeto
    RewriteBase /babyhappy/public/ 

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [L]
</IfModule>